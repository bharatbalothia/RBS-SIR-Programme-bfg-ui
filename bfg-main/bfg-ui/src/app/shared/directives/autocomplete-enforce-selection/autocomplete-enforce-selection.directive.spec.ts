import { AutocompleteEnforceSelectionDirective } from './autocomplete-enforce-selection.directive';

describe('AutocompleteEnforceSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new AutocompleteEnforceSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
